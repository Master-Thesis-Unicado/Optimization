
The optimized descent phase demonstrates an energy-efficient altitude transition strategy that minimizes fuel consumption while achieving rapid altitude loss. The descent profile, computed using dynamic programming optimization, achieves an altitude reduction of $\Delta h = 9,760$ m over a duration of $t_{\text{descent}} = 7.2$ minutes, consuming $m_{\text{fuel,descent}} = 117.34$ kg of fuel. The average descent rate of $\bar{\dot{h}} = 1,839$ m/min represents an efficient vertical profile that balances descent speed with fuel efficiency, utilizing aerodynamic braking and minimal thrust application.

\subsubsection{Thrust and Drag Characteristics}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.85\textwidth]{Images/OptimizedMission/Descent/descent_performance_thrust_drag.png}
    \caption{Descent phase thrust and drag force evolution for optimized mission.}
    \label{fig:descent_thrust_drag}
\end{figure}

The thrust and drag profiles, presented in Figure~\ref{fig:descent_thrust_drag}, reveal a fuel-efficient descent strategy characterized by minimal thrust application and dynamic drag management. Thrust remains at very low levels throughout the majority of the descent, starting at $T_0 = 3$ kN and maintaining values between $1$ and $8$ kN from $0$ to approximately $3.8$ minutes. This near-idle thrust operation minimizes fuel consumption during the initial and mid-descent phases, where gravitational potential energy provides the primary energy source for altitude loss.

Small transient thrust peaks occur around $2.7$ minutes ($T \approx 8$ kN) and $3.2$ minutes ($T \approx 7$ kN), likely representing brief thrust adjustments to maintain desired flight path or airspeed constraints. From approximately $3.8$ minutes onward, thrust increases gradually in an approximately linear fashion, reaching $T_{\text{final}} = 25$ kN at descent termination. This terminal thrust increase indicates a transition toward controlled deceleration and preparation for the approach phase, where increased thrust may be required for speed and flight path management.

Drag exhibits a highly dynamic profile, beginning at $D_0 = 55$ kN and experiencing a sharp increase to a peak of $D_{\max} = 140$ kN at approximately $2.8$ minutes. This substantial drag peak represents an aggressive energy dissipation strategy, likely involving high angles of attack, speed brake deployment, or steep flight path angles to rapidly reduce altitude and airspeed. The drag peak occurs during the high-speed segment of the descent, where aerodynamic braking is most effective.

Following the peak, drag decreases sharply to approximately $75$ kN by $3.2$ minutes, then exhibits further fluctuations including a secondary peak of approximately $85$ kN at $4$ minutes. From approximately $4.5$ minutes to descent termination, drag decreases gradually and stabilizes at $D_{\text{final}} = 48$-$50$ kN. This final drag reduction reflects the transition to lower airspeeds and altitudes, where reduced dynamic pressure naturally decreases drag forces.

Throughout the descent, drag significantly exceeds thrust during the initial and mid-descent phases, confirming that the descent is primarily driven by aerodynamic forces rather than engine thrust. This negative excess thrust (drag exceeding thrust) enables rapid altitude loss while maintaining fuel-efficient operation through minimal thrust application.

\subsubsection{Fuel Flow Characteristics}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.85\textwidth]{Images/OptimizedMission/Descent/descent_performance_fuel_flow.png}
    \caption{Descent phase fuel flow rate profile for optimized mission.}
    \label{fig:descent_fuel_flow}
\end{figure}

The fuel flow rate profile, illustrated in Figure~\ref{fig:descent_fuel_flow}, exhibits significant variation throughout the descent phase, reflecting the dynamic nature of the optimized descent strategy. Initial fuel flow at descent entry is $\dot{m}_0 = 410$ kg/h, gradually increasing to approximately $630$ kg/h at $2.3$ minutes. A more pronounced increase follows, reaching a local peak of $\dot{m} = 980$ kg/h at approximately $2.8$ minutes, coinciding with the drag peak observed in the force profiles.

Following a brief decrease to approximately $760$ kg/h at $3.3$ minutes, fuel flow undergoes a significant surge, peaking at $\dot{m}_{\max} = 1,790$ kg/h just after $4$ minutes ($t \approx 4.1$ minutes). This maximum fuel flow rate represents the highest consumption during the descent phase and likely corresponds to a thrust adjustment or flight path correction maneuver. After this peak, fuel flow decreases steadily, stabilizing at approximately $1,200$ kg/h from $6$ minutes until descent termination at $7.2$ minutes.

The average fuel flow rate during descent is $\bar{\dot{m}} = m_{\text{fuel}} / t_{\text{descent}} = 117.34$ kg / $0.12$ h $= 978$ kg/h, which is significantly lower than climb phase fuel flow ($4,511$ kg/h) but higher than would be expected for an ideal idle descent. This non-zero fuel consumption reflects the fuel required for engine idle operation, auxiliary power units, and the thrust adjustments necessary for flight path and speed control during the optimized descent profile.

\subsubsection{Velocity and Mach Number Profile}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.85\textwidth]{Images/OptimizedMission/Descent/descent_performance_airspeed.png}
    \caption{Descent phase true airspeed evolution for optimized mission.}
    \label{fig:descent_airspeed}
\end{figure}

The true airspeed profile, shown in Figure~\ref{fig:descent_airspeed}, demonstrates a multi-segment descent strategy with significant speed variations. Descent initiation occurs at $V_0 = 230$ m/s, with airspeed increasing rapidly to approximately $248$ m/s within the first $0.5$ minutes. The airspeed continues to increase, reaching a peak of $V_{\max} = 262$ m/s at approximately $2.8$ minutes. This initial acceleration reflects a high-speed descent strategy, where the aircraft maintains or increases speed during the initial altitude loss to exploit the high-speed, high-altitude segment for efficient energy management.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.85\textwidth]{Images/OptimizedMission/Descent/descent_performance_mach.png}
    \caption{Descent phase Mach number profile for optimized mission.}
    \label{fig:descent_mach}
\end{figure}

Following the peak, a sharp deceleration occurs from $2.8$ to approximately $3.8$ minutes, with airspeed decreasing from $262$ m/s to approximately $195$ m/s. This rapid deceleration coincides with the drag peak and represents a transition in descent strategy, likely involving speed brake deployment or flight path angle adjustments to reduce airspeed for lower altitude operations.

After this transition, airspeed maintains a relatively stable level around $195$ m/s before resuming a gradual decrease. The final segment, from approximately $4.2$ minutes to descent termination, shows continuous deceleration from approximately $195$ m/s to $V_{\text{final}} = 88$ m/s. This terminal deceleration prepares the aircraft for approach and landing speeds, where lower airspeeds are required for safe operations.

The Mach number profile, illustrated in Figure~\ref{fig:descent_mach}, reflects the speed management strategy throughout descent. Descent initiation occurs at $M_0 = 0.77$, with Mach number increasing slightly to a peak of $M_{\max} = 0.83$ at approximately $1.1$ minutes. A plateau maintains Mach number around $M = 0.80$ from $1.1$ to $2.5$ minutes, representing a constant Mach number descent segment optimized for the high-altitude portion.

A significant deceleration occurs from $2.5$ to $3.8$ minutes, with Mach number decreasing from approximately $0.82$ to $M = 0.59$. This rapid Mach reduction corresponds to the airspeed deceleration and represents the transition from high-speed descent to lower-speed operations. A brief stabilization at $M \approx 0.59$ occurs from $3.8$ to $4.2$ minutes, followed by continuous deceleration to $M_{\text{final}} = 0.26$ at descent termination.

The progressive Mach number reduction from $0.77$ to $0.26$ demonstrates the optimization algorithm's recognition that lower Mach numbers are required for safe operations at lower altitudes and for approach preparation. This speed reduction strategy balances fuel efficiency (maintaining higher speeds longer) with operational constraints (reducing speed for terminal area operations).

\subsubsection{Throttle Lever Management}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.85\textwidth]{Images/OptimizedMission/Descent/descent_performance_lever.png}
    \caption{Descent phase throttle lever position profile for optimized mission.}
    \label{fig:descent_lever}
\end{figure}

The throttle lever position profile, presented in Figure~\ref{fig:descent_lever}, demonstrates an idle descent strategy throughout the entire descent phase. The throttle setting remains constant at $\delta = 0\%$ for the complete $7.2$-minute descent duration, indicating that the engines operate at minimum idle power throughout the phase. This idle descent strategy maximizes fuel efficiency by minimizing thrust production, relying instead on gravitational potential energy and aerodynamic forces to achieve altitude loss.

The constant $0\%$ throttle setting, well below both the maximum continuous thrust (MCT) limit of $85\%$ and the maximum thrust limit of $100\%$, confirms that the optimized descent prioritizes fuel efficiency over rapid descent capability. This strategy is optimal for fuel minimization, as it eliminates the fuel consumption associated with thrust production during descent, accepting instead the fuel required for engine idle operation and auxiliary systems.

The idle descent strategy is feasible because the aircraft's drag exceeds thrust requirements for descent, enabling altitude loss through aerodynamic forces alone. The optimization algorithm correctly identifies that maintaining idle thrust throughout descent minimizes fuel consumption while still achieving the required altitude reduction and speed management through aerodynamic control.

\subsubsection{Mass and Fuel Consumption}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.85\textwidth]{Images/OptimizedMission/Descent/descent_performance_mass.png}
    \caption{Descent phase aircraft mass evolution for optimized mission.}
    \label{fig:descent_mass}
\end{figure}

Aircraft mass evolution, shown in Figure~\ref{fig:descent_mass}, demonstrates the continuous decrease expected from fuel consumption during descent. Initial mass at descent entry is $m_0 = 62,838$ kg, decreasing to $m_{\text{final}} = 62,720$ kg at descent termination. The mass reduction of $\Delta m = 118$ kg closely matches the fuel consumed ($m_{\text{fuel}} = 117.34$ kg), confirming the accuracy of the fuel consumption calculations.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.85\textwidth]{Images/OptimizedMission/Descent/descent_performance_fuel.png}
    \caption{Descent phase cumulative fuel consumption for optimized mission.}
    \label{fig:descent_cumulative_fuel}
\end{figure}

The cumulative fuel consumption profile, illustrated in Figure~\ref{fig:descent_cumulative_fuel}, shows a monotonic increase from $0$ kg to $117.34$ kg over the $7.2$-minute descent duration. The curve exhibits a slightly accelerating rate of fuel consumption in the initial minutes, corresponding to the increasing fuel flow rates observed during the early descent phase. The cumulative fuel consumption confirms that fuel is continuously consumed throughout descent, reflecting the irreducible fuel requirements for engine idle operation and auxiliary systems, even during fuel-efficient idle descent.

\subsubsection{Three-Dimensional Optimization Trajectory}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.85\textwidth]{Images/OptimizedMission/Descent/descent_J_3d_plot.png}
    \caption{Three-dimensional dynamic programming solution showing optimal descent trajectory within engine operating envelope.}
    \label{fig:descent_3d_envelope}
\end{figure}

The three-dimensional dynamic programming solution, visualized in Figure~\ref{fig:descent_3d_envelope}, demonstrates the globally optimal descent trajectory within the full engine operating envelope. The optimal path, represented by the purple trajectory, navigates the three-dimensional state space defined by altitude ($h$), Mach number ($M$), and throttle lever position ($\delta$), systematically exploring the discretized grid to identify the fuel-minimizing descent path.

The optimal trajectory exhibits a characteristic pattern where the descent progresses from high altitude to low altitude, with Mach number and lever position optimized at each altitude increment. The trajectory maintains relatively high Mach numbers during the initial high-altitude segment, then transitions to lower Mach numbers as altitude decreases, reflecting the optimization algorithm's recognition that speed reduction is necessary for lower altitude operations while maintaining fuel efficiency.

The lever position remains at minimum (idle) throughout the optimal trajectory, confirming that the fuel-minimizing descent strategy utilizes minimal thrust. The trajectory navigates the operating envelope boundaries, operating near but not violating constraints, to maximize fuel efficiency while achieving the required altitude reduction and speed management.

\subsubsection{Performance Metrics}

The optimized descent phase achieves a fuel efficiency of $m_{\text{fuel}}/s_{\text{descent}}$, where the descent distance can be calculated from the descent profile. The descent phase consumes $0.9\%$ of the total mission fuel ($m_{\text{fuel,total}} = 12,712.3$ kg) while covering a relatively small portion of the total mission distance, reflecting the fuel-efficient nature of idle descent compared to powered flight phases.

The average descent rate of $\bar{\dot{h}} = 1,839$ m/min ($30.65$ m/s) represents an efficient vertical profile that achieves rapid altitude loss while maintaining fuel efficiency through minimal thrust application. This descent rate is significantly higher than typical commercial aircraft descent rates (typically $300$-$600$ m/min), reflecting the optimization algorithm's focus on minimizing descent time and fuel consumption rather than passenger comfort or noise abatement.

The descent phase demonstrates the effectiveness of idle descent strategies for fuel minimization. By maintaining throttle at idle ($\delta = 0\%$) throughout the descent and utilizing aerodynamic forces for altitude loss, the optimization algorithm achieves minimal fuel consumption while still meeting operational constraints for altitude reduction, speed management, and approach preparation. This strategy represents a fundamental principle of fuel-efficient flight: exploit gravitational potential energy for altitude changes rather than consuming fuel for thrust production.

 