The optimal trajectory results presented in this section are obtained using a high-resolution dynamic programming configuration selected to maximize fuel efficiency. The optimization employs a fine grid discretization with penalty system parameters chosen based on sensitivity analysis to achieve optimal fuel consumption while maintaining terminal constraint satisfaction.

\begin{table}[h]
    \centering
    \footnotesize
    \caption{Optimization configuration parameters for optimal trajectory results.}
    \label{tab:optimal_trajectory_configuration}
    \begin{tabular}{l c c}
        \hline
        Parameter Category & Parameter & Value \\
        \hline
        \textbf{Grid Discretization} & & \\
        Mach samples (climb/descent) & $N_{MACH}$ & 50 \\
        Altitude steps (climb/descent) & $N_{ALT}$ & 50 \\
        Lever samples (climb/descent) & $N_{LEVER}$ & 50 \\
        Grid resolution & $(K \times I \times L)$ & $50 \times 50 \times 50$ \\
        \hline
        \textbf{Mach Trajectory Guidance} & & \\
        Guidance enabled (climb/descent) & -- & Enabled \\
        Guidance penalty weight & $w_{guidance}$ & 0.3 kg/Mach$^2$ \\
        Urgency multiplier & $\alpha_{urgency,max}$ & 1.8 \\
        \hline
        \textbf{Lever Position Penalty} & & \\
        Lever penalty enabled (climb) & -- & Enabled \\
        Lever penalty weight & $w_{lever}$ & 3.0 kg \\
        \hline
        \textbf{Center of Gravity} & & \\
        Dynamic CG calculation & -- & Enabled \\
        Fuel consumption scenario & -- & Inner First \\
        \hline
    \end{tabular}
\end{table}

Table~\ref{tab:optimal_trajectory_configuration} summarizes the configuration parameters employed for the optimal trajectory analysis. The grid resolution of $50 \times 50 \times 50$ provides high-fidelity discretization of the three-dimensional state space (Mach number, altitude, throttle lever position), enabling the optimizer to exploit fuel-efficient trajectory segments with minimal discretization error. This grid density achieves fuel consumption within 2.9\% of the finest tested grid ($55 \times 55 \times 55$) while maintaining reasonable computational cost. The Mach trajectory guidance system employs baseline penalty weight $w_{guidance} = 0.3$ kg/Mach$^2$, providing effective trajectory guidance without excessive fuel penalties. The lever position penalty system utilizes weight $w_{lever} = 3.0$ kg to moderate throttle lever usage patterns. The inner-first fuel consumption scenario is selected to maximize fuel efficiency, achieving the lowest fuel consumption among the evaluated depletion strategies.
