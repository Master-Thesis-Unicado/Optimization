Mission-level optimization extends trajectory optimization to higher-level mission planning objectives, including range targeting and minimum fuel capacity determination. These analyses demonstrate the system's capability to solve inverse problems where mission requirements (target range, fuel capacity constraints) are specified and the optimizer determines corresponding trajectory parameters (cruise distance, fuel loading) to satisfy these requirements.
