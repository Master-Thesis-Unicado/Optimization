Aircraft trajectory dynamics are modeled using a point-mass formulation that captures the physics while maintaining computational tractability. The three-degree-of-freedom (3-DOF) model considers translational motion in three spatial dimensions, assuming the aircraft behaves as a point mass with orientation aligned to the velocity vector.
